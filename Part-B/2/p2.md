1. Load the excel workbook in a blank report 
2. Ensure all the data, Date is in Date Format, Revenue and Quantity is in Whole Number 
3. Rename the whole dataset as Ecomm
4. Create a new column: 
    1. Year = YEAR(Ecomm[Order Date]) 
5. Create new measures: 
    1. total order = DISTINCTCOUNT(Ecomm[Order_ID])
    2. total revenue = SUM(Ecomm[Revenue]) 
    3. aov = DIVIDE([total revenue], [total order], 0)
6. Create bar charts for total order by category, location and age group 
7. Create a donut chart add total orders and then in legend add order status for it 
8. Add slicers for Region(Dropdown styled), Location(dropdown) and Category(tile styled) 
9. Ensure its placed well in the report and test the interactivity