1. Load the data as CSV from the file 
2. Ensure the Date Column is in Date format and other Sales and Profit are in numeric format  
3. Rename dataset as SalesData
4. Create new columns: 
    1. Year = YEAR(SalesData[Order Date]) 
    2. Month = MONTH(SalesData[Order Date]) 
    3. Month no = FORMAT(SalesData[Order Date], “mmm”)
5. Sort by Ascending the Month 
6. Create new measures: 
    1. total sales = SUM(SalesData[Sales])
    2. total profit = SUM(SalesData[Profit]) 
    3. sales ly = CALCULATE([total sales], SAMEPERIODLASTYEAR(SalesData[Order Date]))
    4. sales growth = DIVIDE([total sales], - [sales ly], [sales ly], 0)
7. Create Bar charts for total sales by region, cat, sub cat (x→total sales, y→ region, cat, sub cat)(drag the region, sub cat and cat first) 
8. Create Line charts for total sales by year, month 
9. Create filters 
    1. Year
    2. Region 
    3. Category 
10. Ensure everything is placed well in the report and test interactivity